name: Node.js CI

on:
  push:
    branches:
    - production
  pull_request:
    branches:
    - production

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - run: npm install
